export interface Personal {
    greeting : string,
    pic : string,
    signature : string,
    myemail : string }
    export interface Sentiment {txt: string, pic: string}
    export interface Border {colour: string}
    export interface BorderEdge {edge: string}
    
    